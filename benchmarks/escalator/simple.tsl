always assume {
	!([up <- True()] && [up <- False()]);
}

always assume {
	[up <- True()] || [up <- False()];
}

always guarantee {
	gt bottom top -> [up <- True()];
	!(gt bottom top) -> [up <- False()];
}
